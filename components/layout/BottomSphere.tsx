import React from "react";

import classes from "./BottomSphere.module.css";

type Props = {
	children?: React.ReactNode;
	// props....
};

//todo:-----BottomSphere component-----://
const BottomSphere = (props: Props) => {
	return (
    <div className={classes.container}>-</div>
	);
};

export default BottomSphere;
